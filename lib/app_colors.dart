import 'package:flutter/material.dart';

class AppColors {
  static const blueText = Color(0xFF2C348B);  // Deep blue for all text
  static const redText = Color(0xFFC62828);   // Red for 'T' in Together!
  static const background = Color(0xFFF4F4FD); // Soft lavender background
  static const navbar = Color(0xFFDDE3F5);     // Light blue for nav and cards
  static const button = Color(0xFF2C348B);     // Primary button color
}
